# 从这次生成里能学到的 agent 知识

这份笔记不是教程目录，而是把"我刚才到底怎么做出 `index.html` 的"拆开来，
让你以后能自己判断一个 agent 干得好不好、也能把要求提得更省事。

---

## 1. 一个功能落地的顺序：模型 -> 骨架 -> 细节 -> 自检

我实际按这个顺序做事，顺序本身就是经验：

1. **定行为边界**：问清"本地单文件 / 只给我自己用"，于是砍掉后端、账号、同步。
   边界定得越早，浪费的代码越少。
2. **先定数据结构，再写界面**。这是最关键的一步：

   ```js
   { habits: [ { id, name, done: { '2026-09-20': true } } ] }
   ```

   把"哪天完成"写成以日期为键的表，带来三个白送的好处：
   - 跨天自动重置：新的一天用新键，不需要凌晨跑定时任务去清空。
   - 历史天然保留：7 天格子、连续天数都是从同一份数据算出来的，不用另存。
   - 点亮是幂等操作：`done[key] = true` 执行几次结果都一样，不怕重复点击。

   **你以后验收一个 agent 的产出，可以先问它一句"数据结构是什么"。**
   如果它答不上来、或者一边写界面一边临时加字段，后面大概率会返工。

3. **骨架先跑起来**：HTML 结构 + 渲染函数，能画出空列表就算通了。
4. **再加交互和反馈**：进度条、+1 飘字、全部完成的小庆祝。
5. **最后自检**（见第 3 节）。

---

## 2. 代码里的几个工程取舍

| 选择 | 为什么这么做 | 代价 |
| --- | --- | --- |
| 单个 HTML 文件，零依赖 | 双击就能用，不联网、不装环境；你随时能打开读 | 功能变大后会变得难维护，那时就该拆文件了 |
| 每次改数据就整体重画列表 | 代码短、不会出现"界面和数据不一致"的 bug | 习惯几百个时会卡，个人用途到不了 |
| localStorage 存数据 | 浏览器自带，无需服务端 | 换浏览器/清缓存就没了，所以要慎点"清空" |
| 读数据时做一次清洗 | 存坏了也不至于白屏，最差是回到初始状态 | 多十几行代码 |

这就是"够用优先"：先跑通，再为真实需要付复杂度。

---

## 3. 我这次是怎么自检的（这才是 agent 最该有的部分）

我没法用眼睛看你的浏览器，所以用无头 Chrome 做了三层验证。你可以让任何 agent 都这么干：

1. **语法与引用检查**：用 node 解析脚本，确认没语法错，并且代码里
   `getElementById` 用到的每个 id 在 HTML 里都真实存在（漏一个就是运行时崩溃）。
2. **真实交互测试**：把页面复制一份，注入一段"模拟用户操作"的脚本——
   添加 3 个习惯、点亮、跨天数据——再让 Chrome 把执行后的 DOM 打印出来核对。
   核对结果：进度 `3 / 3`、连续天数 `4` / `2`、7 天格子共 21 格、庆祝横幅出现。
3. **多宽度布局体检**：在 320 / 375 / 560 像素宽的窗口里测量每个元素，
   确认横向没有溢出、卡片里没有元素越界、7 个格子互不重叠。

**结论：让 agent"自证"，而不是让它说"应该可以"。**
一句好用的要求是："改完请自己跑一遍验证，并把验证结果贴给我。"

顺带一个诚实的说明：我这次**没能亲眼看到渲染效果**（我看不了图片），
只做到了数值层面的确认。如果你打开后觉得哪里难看，直接说"太挤/太小/颜色太跳"，
这类纯视觉问题一句话就能改。

---

## 4. 你以后怎么提要求更省事

有效的说法长这样，四段就够：

- **目标**：我想要一个能每天点亮习惯的页面
- **约束**：单文件、本地、不联网、只有我自己用
- **验收**：能添加/删除习惯，能看到连续天数和最近 7 天，刷新不丢数据
- **不要什么**：不要登录、不要后端、不要图表

反过来，这些说法会拖慢进度：

- "做一个好看的习惯 App"——好看没标准，agent 只能猜。
- "你先写 CSS 再写 JS 再调间距"——给步骤等于替它做技术决策，容易撞车。
  给**目标和约束**，把实现方式留给它。
- 一次提十件事。**一次只改一件事**，每次改完你能看清 diff，也知道是哪一步坏了。

还有一条很实用：**遇到 bug，给我"现象 + 复现步骤"就够了**，
比如"点了删除没反应，按钮在卡片右上角"，不需要你自己判断原因。

---

## 5. 接下来可以练的改动（由易到难）

1. 改文案和颜色：`--accent` 换一个色，标题换成你喜欢的字。
2. 加"一个习惯可以设每周目标次数"，比如"每周 3 次"。
3. 加"补卡"：允许点 7 天格子里任意一格（现在只能点今天）。
4. 加数据导出/导入 JSON（换电脑或备份用）。
5. 把单文件拆成 `index.html` + `app.js` + `style.css`，体会一下"为什么工程大了要拆"。

每做一步，让我或别的 agent 改完顺手跑一遍第 3 节的验证，你就有一整套可复用的流程了。

---

## 6. 数据存在哪、怎么彻底清掉

- 位置：浏览器为这个文件保存的 localStorage，键名 `habit-tracker-v1`。
- 手动清除：页面底部"清空全部"，或者浏览器 F12 -> Application -> Local Storage。
- 换浏览器、换电脑、无痕模式都会看到空列表，属正常现象。

---

## 7. 加第二个功能时，怎么做到"页面简单、代码也简单"

第二轮加了坏习惯计数（今天发生几次、控制住几次）。同一份文件从 300 多行变成 400 多行，
但核心思路一行都没推翻。这次的四条经验值得记住：

**1. 让新功能复用旧概念，而不是新造一套。**
好习惯用 `done: { '2026-09-20': true }` 记录"哪天完成"，坏习惯就用
`log: { '2026-09-21': { hit: 2, held: 1 } }` 记录"那天各几次"。同样是日期当键，于是：
每天自动归零（新日期是新键，不需要任何清零代码）、历史自动保留、
"累计次数"只是把这个表加起来的三行函数。**如果当初图省事写成 `{ hit: 2, held: 1, date: '2026-09-21' }`，
就必须自己写跨天判断逻辑，而且看不到历史。**

**2. 新界面尽量挂到已有样式上。**
坏习惯的卡片直接用了好习惯的 `.habit` 类，白捡了卡片、标题、删除按钮的全部样式，
新增的 CSS 只有计数条那十来行。加功能时先问："现有样式能复用哪一层？"

**3. 不新增颜色。**
"控制住了"是好事，但它没引入绿色——复用了好习惯点亮的那个强调色 `--accent`。
颜色越少，页面越安静，改主题时也只需要改一个变量。

**4. 小地方的两个取舍，都很典型：**
- 点错了怎么办？没做"撤销栈"（那是另一种复杂度），只给每个数字配了一个小 `−`，
  数字是 0 时它隐藏。**用最小代价换取"我可以随时纠正"的安全感。**
- 数字为 0 时减号用 `visibility: hidden` 而不是 `display: none`，
  因为它要占位——否则每次数字变化，后面的内容都会左右跳一下。
- 计数减回 0 就把那天的记录删掉，不让空记录在数据里堆积。

**这次的验证结果**（同样是无头 Chrome 跑出来的）：
计数按天累加正确（昨天 3 次 + 今天 2 次 = 累计 5 次）、减号在 0 时不显示、
减到 0 不留空记录、好习惯的连续天数和 7 天格子不受影响、
320 / 375 / 560 三种宽度下没有溢出和重叠。

---

## 8. 把一句"感觉"翻译成 CSS（液态玻璃那次改动）

你说的是"iOS 液态玻璃、像 Apple 官网、白色主导"。这句话对人是清楚的，对代码是没用的，
所以 agent 要做的是把它拆成可执行的具体决定。这次拆成了五个：

**1. 颜色不靠猜，直接抄 Apple 的取值。**
正文黑 `#1d1d1f`、次级灰 `#6e6e73`、强调蓝 `#0071e3`、系统红 `#d70015`。
这些是 Apple 官网和 iOS 实际在用的色值，比自己调出来的"差不多"更接近你要的观感。
**学到的一点：遇到"要某某风格的"，最好的办法是去找这个风格的官方色值和数值，而不是凭感觉配。**

**2. 玻璃的关键不是 `backdrop-filter`，而是"背后有没有东西可以虚化"。**
这是这次改动里最重要的一句。纯白底上的毛玻璃看上去和普通白卡片几乎一样，
所以背景必须有一点极淡的颜色——四层 `radial-gradient`（蓝、粉、紫、青，透明度只有 9%~14%），
再用 34 秒的极慢位移动画让它"流"起来。这一层才是"液态"的来源。

**3. 玻璃的质感靠四件小东西叠出来，缺一件就假：**

| 手法 | 作用 |
| --- | --- |
| `backdrop-filter: blur(24px) saturate(180%)` | 模糊 + 提饱和度，让背后的颜色"透"上来 |
| `inset 0 1px 0 rgba(255,255,255,.9)` | 顶部一道白色高光，模拟玻璃的上边缘 |
| `0 0 0 1px rgba(0,0,0,.045)` | 极细的外沿，让卡片在白底上有边界 |
| 大圆角 + 极浅的投影 | 让卡片"浮"在背景上 |

**4. 动效要克制，但必须存在。**
点亮圆点时的反馈被换成了三件事：圆点缩小再弹回、白色对勾浮出、一圈光晕扩散出去。
原来撒粒子的庆祝删掉了，换成横幅出现时划过一道光。**判断标准是：反馈用来确认"动作被接收了"，
不是用来表演。** 同时保留 `prefers-reduced-motion`，系统开了减弱动效就全部关掉。

**5. 换皮不换骨。**
这次只动了 CSS，数据结构、localStorage 格式、渲染函数、行为逻辑一行没改
（JS 只删掉了一个不再需要的函数和两个坐标数字）。**改视觉和改功能应该是两件独立的事**，
能分开就分开，出问题时你才知道是哪一边坏了。

**视觉改动的验证方式**（因为看不了图片，只能靠数值）：
用浏览器读真实计算后的样式，确认毛玻璃生效（`blur(24px) saturate(1.8)`）、
背景确实有四层色晕、正文色是 `rgb(29,29,31)`、点亮后对勾的不透明度是 1、
庆祝横幅的扫光动画在运行；
再在 320 / 375 / 430 / 560 四种宽度下量布局，确认七天格子没有溢出卡片、没有横向滚动条。
