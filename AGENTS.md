# 这个仓库的工作约定

给未来的 agent（包括新会话里的我）的说明。用户明确提出的两条，请照做。

## 1. 提改动建议之前，先提问

用户提出任何改动建议时，**先问 2-3 个真正影响结果的问题，再动手**。

- 每个问题都给出你推荐的默认值，并说明理由，这样用户可以只回"按你推荐的来"。
- 用开放式的问法，不要写成选择题菜单。
- 问题要问在真正有分歧的地方（视觉取向、交互方式、取舍），不要问能自己判断的小事。
- 得到答复后再实现。

## 2. 视觉方向

白色主导、极简、iOS「液态玻璃」质感，参考 Apple 官网的观感：

- 大圆角、半透明白卡片 + 背景模糊、细白色高光边、极轻的阴影。
- 玻璃层次需要背景有一点极淡的色彩，不要做成死白平面。
- 强调色、字号、间距保持克制；信息只留必要的。

## 3. 代码约定

- 全部写在单个 `index.html` 里，纯 HTML/CSS/JS，**不引入任何依赖、构建步骤或网络字体**（要能离线双击打开）。
- 保持极简：新功能尽量复用已有的数据结构和样式，不新造一套概念。
- 数据存在 localStorage，键名 `habit-tracker-v1`。**改动必须向后兼容**，不能弄丢用户已有的记录。
- 中文注释，保留"数据 -> 渲染 -> 行为"三段式结构。

## 4. 交付前必须自检

改完不要只说"应该可以"，用无头 Chrome 实测并报告结果：

1. 语法检查 + 确认代码里引用的每个元素 id 都真实存在。
2. 把页面复制一份、注入模拟操作的脚本，跑一遍交互，核对数据与渲染结果（含老数据兼容）。
3. 在 320 / 375 / 560 像素三种宽度下量布局，确认没有横向溢出、重叠、越界。

看不清或没验证到的部分要如实说明（比如看不了图片就别假装看过渲染效果）。

## 5. 发布

- 仓库：`chung0302/habit-catch`，主分支 `main`，GitHub Pages 从 main 的根目录发布。
- 更新方式：改完 commit + push，同一个网址自动更新（CDN 缓存约 10 分钟，需要时 Ctrl+F5）。
- 注意：localStorage 按网址隔离，本地文件版和网页版是两套独立数据。
